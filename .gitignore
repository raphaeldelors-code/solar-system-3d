node_modules/
dist/
*.local
.DS_Store
npm-debug.log*

# Chrome / dev-server crash dumps (large binaries; eslint chokes reading them).
core.*

# LLM-judge review artifacts (scratch; regenerated by the review loop).
.judge/

# Heavy source plates + intermediate extraction output (regenerable via
# assets/constellation-figures/mkfig2.py; not shipped, served from public/).
assets/constellation-figures/src/
assets/constellation-figures/out/
assets/constellation-figures/previews/
